import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─────────────────────────────────────────────
  // Common
  // ─────────────────────────────────────────────

  static const Color navy = Color(0xFF0A2A66);
  static const Color blue = Color(0xFF1769D2);
  static const Color lightBlue = Color(0xFFEAF1FF);

  static const Color background = Color(0xFFF6F8FC);
  static const Color darkText = Color(0xFF172033);
  static const Color textGrey = Color(0xFF64748B);
  static const Color borderColor = Color(0xFFE2E8F0);

  static const Color inputBackground = Color(0xFFF8FAFD);

  static const Color white = Colors.white;
  // Transaction colors
  static const Color incomeGreen = Color(0xFF159957);
  static const Color expenseRed = Color(0xFFE5484D);

  static const Color lightBlueBackground = Color(0xFFEAF1FF);
  static const Color emptyStateBlue = Color(0xFFDCE9FF);

  static const Color grey100 = Color(0xFFF1F4F8);
  static const Color grey500 = Color(0xFF64748B);

  // ─────────────────────────────────────────────
  // Transaction
  // ─────────────────────────────────────────────

  static const Color income = Color(0xFF159957);
  static const Color incomeBackground = Color(0xFFE8F7EF);

  static const Color expense = Color(0xFFE5484D);
  static const Color expenseBackground = Color(0xFFFFEEEE);

  static const Color transactionIconBackground = Color(0xFFF1F4F8);

  // Exact Material grey shades used in transaction UI.
  static const Color grey200 = Color(0xFFEEEEEE);
  static const Color grey400 = Color(0xFFBDBDBD);
  static const Color grey600 = Color(0xFF757575);
  static const Color grey700 = Color(0xFF616161);

  // White transparency values used by transaction UI.
  static const Color white70 = Colors.white70;
  static const Color white13 = Color(0x21FFFFFF);

  // ─────────────────────────────────────────────
  // Transaction Header
  // ─────────────────────────────────────────────

  static const Color transactionHeaderStart = Color(0xFF0A2A66);
  static const Color transactionHeaderEnd = Color(0xFF1769D2);
}